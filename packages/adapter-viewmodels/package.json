{
  "name": "@repo/adapter-viewmodels",
  "version": "0.0.0",
  "private": true,
  "description": "ViewModels adapter - Inbound adapter containing ViewModels that will be connected to UI presentational (dumb) components",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --max-warnings 0",
    "type-check": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@dxbox/use-less-react": "^0.13.0",
    "@repo/domain": "workspace:*"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "@repo/config-typescript": "workspace:*",
    "@repo/config-vitest": "workspace:*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.0"
  }
}
