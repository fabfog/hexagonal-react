{
  "name": "@repo/ports",
  "version": "0.0.0",
  "private": true,
  "description": "Interfaces and contracts - Ports layer (hexagonal architecture)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo 'Ports package contains only types - no build needed'",
    "dev": "tsc --noEmit --watch",
    "lint": "eslint . --max-warnings 0",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/domain": "workspace:*"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "@repo/config-typescript": "workspace:*",
    "typescript": "^5.9.3"
  }
}
