/**
 * {{pascalCase name}} Port (Interface)
 *
 * Defines the contract for {{name}}.
 * Implementations (adapters) must satisfy this interface.
 */
export interface {{pascalCase name}} {
  // TODO: Define port methods
}
