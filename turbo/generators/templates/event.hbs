import { DomainEvent } from "@dxbox/use-less-react/classes";

export interface {{pascalCase name}}EventPayload {
  // TODO: Add event payload fields
}

export class {{pascalCase name}}Event extends DomainEvent<"{{pascalCase name}}", {{pascalCase name}}EventPayload> {
  get type(): "{{pascalCase name}}" {
    return "{{pascalCase name}}";
  }

  constructor(payload: {{pascalCase name}}EventPayload) {
    super(payload);
  }
}
