import { DomainEvent } from "@dxbox/use-less-react/classes";

export interface {{pascalCase name}}Payload {
  // TODO: Add event payload fields
}

export class {{pascalCase name}}Event extends DomainEvent<"{{pascalCase name}}", {{pascalCase name}}Payload> {
  get type(): "{{pascalCase name}}" {
    return "{{pascalCase name}}";
  }

  constructor(payload: {{pascalCase name}}Payload) {
    super({
      payload,
    });
  }
}
