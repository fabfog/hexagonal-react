import { Command } from "@dxbox/use-less-react/classes";

export interface {{pascalCase name}}Payload {
  // TODO: Add command payload fields
}

export class {{pascalCase name}}Command extends Command<"{{pascalCase name}}", {{pascalCase name}}Payload> {
  get type(): "{{pascalCase name}}" {
    return "{{pascalCase name}}";
  }

  constructor(payload: {{pascalCase name}}Payload) {
    super({
      id: crypto.randomUUID(),
      timestamp: new Date(),
      payload,
    });
  }
}
