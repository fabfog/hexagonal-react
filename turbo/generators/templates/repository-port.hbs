import type { {{pascalCase entity}}, Create{{pascalCase entity}}Data, Update{{pascalCase entity}}Data } from "@repo/domain";

/**
 * {{pascalCase entity}} Repository Port (Interface)
 *
 * Defines the contract for {{entity}} persistence.
 * Implementations (adapters) must satisfy this interface.
 */
export interface {{pascalCase entity}}RepositoryInterface {
  /**
   * Find {{entity}} by ID
   */
  findById(id: string): Promise<{{pascalCase entity}} | null>;

  /**
   * Find all {{entity}}s
   */
  findAll(filters?: Record<string, any>): Promise<{{pascalCase entity}}[]>;

  /**
   * Create a new {{entity}}
   */
  create(data: Create{{pascalCase entity}}Data): Promise<{{pascalCase entity}}>;

  /**
   * Update an existing {{entity}}
   */
  update(id: string, data: Update{{pascalCase entity}}Data): Promise<{{pascalCase entity}} | null>;

  /**
   * Delete a {{entity}}
   */
  delete(id: string): Promise<boolean>;
}
