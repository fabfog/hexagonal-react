import { Query } from "@dxbox/use-less-react/classes";

export interface {{pascalCase name}}QueryPayload {
  // TODO: Add query parameters
}

export type {{pascalCase name}}QueryResult = {
  // TODO: Add query result properties
} | null

export class {{pascalCase name}}Query extends Query<"{{pascalCase name}}", {{pascalCase name}}QueryPayload, {{pascalCase name}}QueryResult> {
  get type(): "{{pascalCase name}}" {
    return "{{pascalCase name}}";
  }

  constructor(payload: {{pascalCase name}}QueryPayload) {
    super(payload);
  }
}
